/*
.WAP to input two 2d matrix, add, subtract and print it. 3.WAP to input two 2d matrix, multiply and print it.
*/
#include<stdio.h>
void main()
{
    int x,y,x1,y1;
    printf("Enter the size of first matrix:");
    scanf("%d%d",&x,&y);
    printf("Enter the size of second matrix:");
    scanf("%d%d",&x1,&y1);
    int m1[x][y];
    if(x=x1 && y==y1)
    {
        
    }
}