/**
WAP to input the length & breadth of a rectangle,
and calculate the area of the rectangle.
**/
#include<stdio.h>
void main()
{
    float l,b;
    printf("Enter the length and breadth:");
    scanf("%f%f",&l,&b);
    area=l*b;
    printf("Area of rectangle is=%.2f cm*cm",area);

}

